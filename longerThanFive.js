// Create a function called longerThanFive that takes a string as it's first argument. console.log true if the string is longer than 5 characters, false if not.
// the letter L ("less than" symbol is "<")
// The "greater than" symbol is ">".
 function longerThanFive(str){
   if (str.length > 5) {

     console.log('true');
   }
   else
   {
     console.log('false');
   }
 }
// longerThanFive('james')
// longerThanFive('jam')
